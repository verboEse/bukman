# Agent Instructions

## Pull Request Targeting

- Always create pull requests against the origin repository.
- Always use `master` as the base branch.
- Never create pull requests against upstream.
- If both `origin` and `upstream` remotes exist, use `origin` for both push and PR creation.
